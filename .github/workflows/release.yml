# Placeholder release workflow.
#
# This file should be regenerated by `dist init` (from `cargo-dist`) once
# the tool is installed locally. Running `dist init` will overwrite this
# file with the canonical multi-platform release pipeline (tarballs,
# installer scripts, checksums, Homebrew formula, etc.).
#
# Until then, this stub exists so `actionlint` can parse the workflows
# directory without errors. It intentionally has no triggers that fire.
name: Release

on:
  workflow_dispatch:

jobs:
  placeholder:
    name: placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Run 'dist init' locally to generate the real release workflow."
